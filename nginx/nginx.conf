# Nginx configuration for CityResQ360 Production
# Đặt file này vào /etc/nginx/sites-available/cityresq360
# Tạo symlink: ln -s /etc/nginx/sites-available/cityresq360 /etc/nginx/sites-enabled/

# Upstream cho các services
upstream coreapi {
    server localhost:8000;
}

upstream app_mobile {
    server localhost:3000;
}

upstream media_service {
    server localhost:8004;
}

upstream notification_service {
    server localhost:8006;
}

upstream wallet_service {
    server localhost:8005;
}

upstream incident_service {
    server localhost:8001;
}

upstream iot_service {
    server localhost:8002;
}

upstream aiml_service {
    server localhost:8003;
}

upstream search_service {
    server localhost:8007;
}

upstream floodeye_service {
    server localhost:8008;
}

upstream analytics_service {
    server localhost:8009;
}

upstream context_broker {
    server localhost:1026;
}

# Main domain - App Mobile (Next.js)
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;

    # Main app
    location / {
        proxy_pass http://app_mobile;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# API Domain - CoreAPI
server {
    listen 80;
    server_name api.yourdomain.com;

    # CoreAPI
    location / {
        proxy_pass http://coreapi;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Admin panel
    location /admin {
        proxy_pass http://coreapi;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Media Service
server {
    listen 80;
    server_name media.yourdomain.com;

    location / {
        proxy_pass http://media_service;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Notification Service
server {
    listen 80;
    server_name notification.yourdomain.com;

    location / {
        proxy_pass http://notification_service;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Wallet Service
server {
    listen 80;
    server_name wallet.yourdomain.com;

    location / {
        proxy_pass http://wallet_service;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Incident Service
server {
    listen 80;
    server_name incident.yourdomain.com;

    location / {
        proxy_pass http://incident_service;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# IoT Service
server {
    listen 80;
    server_name iot.yourdomain.com;

    location / {
        proxy_pass http://iot_service;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# AI/ML Service
server {
    listen 80;
    server_name aiml.yourdomain.com;

    location / {
        proxy_pass http://aiml_service;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Search Service
server {
    listen 80;
    server_name search.yourdomain.com;

    location / {
        proxy_pass http://search_service;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# FloodEye Service
server {
    listen 80;
    server_name floodeye.yourdomain.com;

    location / {
        proxy_pass http://floodeye_service;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Analytics Service
server {
    listen 80;
    server_name analytics.yourdomain.com;

    location / {
        proxy_pass http://analytics_service;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
